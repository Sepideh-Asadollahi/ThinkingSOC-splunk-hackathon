[install]
is_configured = 1
state = enabled

[ui]
# No views shipped (UI lives in the separate frontend); Splunk requires at least
# one view for visible apps, so we hide this app from the launcher.
is_visible = false
label = ThinkingSOC_Hackathon_Splunk_App

[launcher]
author = ThinkingSOC
version = 0.2.0
description = ThinkingSOC hackathon alert handoff to external ingest API.

[package]
id = ThinkingSOC_Hackathon_Splunk_App
check_for_updates = false
