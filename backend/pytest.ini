[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths = tests
markers =
    splunk_live: live Splunk MCP/SAIA integration (requires TSOC_RUN_SPLUNK_LIVE=1)
    real_startup: run FastAPI lifespan startup (slow; may touch Splunk/RAG/embeddings)
