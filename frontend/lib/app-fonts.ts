/**
 * Must stay in sync with `app/layout.tsx` (next/font requires literal `variable` strings).
 */
export const APP_SANS_FONT_VARIABLE = "--font-inter"
export const APP_MONO_FONT_VARIABLE = "--font-mono-family"

/** Classes applied on `<html>` in RootLayout. */
export const LAYOUT_FONT_CLASS_NAMES = ["font-sans", "antialiased"] as const
