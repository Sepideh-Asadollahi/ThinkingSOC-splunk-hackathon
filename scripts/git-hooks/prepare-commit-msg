#!/bin/sh
# Strip Cursor agent co-author trailers before a commit is finalized.
# Enable: bash scripts/setup-git-hooks.sh
sed -i '/cursoragent@cursor\.com/d' "$1"
sed -i '/^Co-authored-by: Cursor /d' "$1"
